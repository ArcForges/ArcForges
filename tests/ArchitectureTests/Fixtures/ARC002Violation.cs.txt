// SPDX-License-Identifier: AGPL-3.0-only
// ARC-002: a ViewModel driving the RPC transport directly instead of going through an Application facade.

namespace ArcChat.Presentation.Fixture
{
    internal sealed class ConversationListViewModel
    {
        private StreamJsonRpc.JsonRpc? _rpc;

        public bool IsConnected => _rpc is not null;
    }
}
