// SPDX-License-Identifier: AGPL-3.0-only
// ARC-011: an upstream FFmpeg struct crossing into managed code. The upstream type is declared here
// because it must never be bindable from managed code in the first place; the rule matches its identity.

public struct AVFrame
{
    public int Width;
    public int Height;
}

namespace ArcForges.NativeInterop.Fixture
{
    public sealed class DecodedSurface
    {
        public AVFrame Frame;
    }
}
