# Step 00 Review Validation Evidence

This record belongs to the review of Step 00 (initial review merged via PR #18; refreshed by the
2026-08-27 review). It is docs-only evidence; no source repository, solution, project, C#/XAML,
build, CI, executable, service, installer, updater, or subprocess was changed or launched.

## Repository and policy checks

- Implementation of record: merged via PR #18 (branch `feat/af00-scope-source-inventory-reboot`,
  merge commit `a2fa9273893878a8642b5bf96aeb4d6de5ddec29`) — closed history, never reopened or reused.
- Current review branch: `feat/af00-scope-source-inventory-review`.
- Current review worktree: `C:\MyFile\ArcForges\ArcForges\.worktree\af00-scope-source-inventory-review`.
- Frozen target base: `origin/main` (`bad05dfb2d0d314a2a5cf9b2edb4ff09f3e8adb8`).
- Source baseline authority remains the six frozen commits recorded in `docs/scope/source-baseline.md`;
  the three declared dirty overlays have DiffSha256 values recorded in the planning-repository
  matrix writeback `1a918eb9f2f287139c642bcf247662bbae9d7d9a` and re-confirmed below.
- `git ls-files '*.ps1' '*.sh'` → empty output (tracked-helper-script policy satisfied).
- `git diff --check` → no output (whitespace check passed).

## Source and inventory assertions

- The current target `docs/scope/source-subsystems.md` has 41 ipcBridge export-group rows. This
  review re-parsed the source: `AionUi/packages/desktop/src/common/adapter/ipcBridge.ts` (baseline
  `29c9271a…`) has 2039 lines and 41 top-level `export const … = {` groups whose names match the
  §1.1 index (`AF-F-AIONUI-0001..0041`) one-for-one.
- The group declarations name 315 member tokens; the group index is explicitly not the Feature
  denominator. The final atomic container remains the planning-repository
  `feature-inventory-and-mapping.md`.
- The six-source baseline and dirty-file re-verification was performed as read-only Git/Python
  checks (no source mutated; the repository policy forbids retaining helper scripts). Recorded
  state: six frozen HEADs match the registered commits; AFFiNE, siyuan, and Serial-Studio are clean;
  AionUi (2), ArcVideo (2), and ArcVideoFoundation (1) carry exactly the five registered dirty
  files. Tracked-file denominators re-confirmed (`git ls-files | wc -l`): 1968 / 10056 / 2538 /
  3748 / 2436 / 48. The normalized `DiffSha256` values were recomputed (raw `git diff` UTF-8 bytes
  minus the single trailing newline) and match `source-coverage-register.md` §2 exactly: AionUi
  `1f87a590859586a4b64831d54b0292117294dabaef93c71a5fe4bcd377525492`, ArcVideo
  `3302eb6c34d252be675ada195461f746c8965d1c2014280df52e0d51a767030c`, ArcVideoFoundation
  `9ccbbea307352499f5ad55ed6f5eb52176f5f0eff7c20df6d3e77969a9f85a96`. Zero drift.
- Dirty feature rows remain `NeedsRecheck`: `AF-F-AIONUI-0283`, `AF-F-AIONUI-0285`,
  `AF-F-ARCV-0065`, `AF-F-ARCV-0069`, and `AF-F-ARCVF-0011`; coverage rows remain
  `SC-AION-03`, `SC-AION-05`, `SC-AV-01`, `SC-AV-04`, and `SC-AVF-02`.

## Packaged oracle

- Static inventory root: `C:\MyFile\ArcForges\StartArcForges`.
- Status: `NotExecuted`; no executable, service, installer, updater, or subprocess was launched.
- Full-tree inventory: 11,297 regular files, 4,172,582,286 bytes.
- Deterministic sorted `product-relative-path|file-length` manifest SHA-256:
  `0e16e92490e1986b71f2b0186ee0a0fe4212119e5eade4b0b3d7f35cd95b143d`.
- Product counts and selected metadata are recorded in
  `docs/scope/packaged-oracle-static-evidence.md`.

## Traceability bridge

- The old ignored local bridge was read and rejected as stale: 24 records, 86 feature IDs, 27
  coverage IDs, abbreviated baselines, null evidence hashes, and placeholder feature IDs.
- A replacement was generated by an inline read-only PowerShell assertion from the current plan
  Feature inventory and Coverage register (the generator is intentionally not persisted because
  tracked `.ps1` files are prohibited).
- Committed artifact re-parsed by this review: `records=24`, unique `featureIds=833`, unique
  `coverageIds=27`, `closureState=BridgeGenerationRequired`; all records remain honest
  `NeedsRecheck`/unclosed until their referenced requirement, test, gate, and source review evidence
  exists. The 24 records equal the 24 Markdown `TR-*` rows in `docs/traceability-matrix.md`.
- Artifact SHA-256 (raw file bytes, reproducible via
  `sha256sum artifacts/evidence/traceability/feature-trace-bridge.json`):
  `f4a8f47549a96e529af5af9582f07a81dd97af73f006ba3c4436c624210a0976`.
  (2026-08-27 correction: the value recorded by the initial review,
  `e7e5768f…74834b41`, was malformed and did not match the committed file; it is replaced by the
  reproducible raw-bytes hash above. The bridge JSON content itself is unchanged.)
- Artifact path: `artifacts/evidence/traceability/feature-trace-bridge.json`.

## Planning-repository writeback

- `license-and-reuse-matrix.md` §1 was corrected by the initial review to reflect the three dirty
  source worktrees and their exact DiffSha256 values; that planning worktree is clean at commit
  `1a918eb9f2f287139c642bcf247662bbae9d7d9a`. This review confirms the plan chain is still
  consistent and adds no new planning-repository writeback or PR.
- This review opens exactly one new target-repository PR for the corrections above; it does not
  claim Step 00 closure. Merged PR #18 is untouched.

## Gate result

The document, license, manifest, oracle, sequencing, and bridge-structure findings were reconciled;
this review additionally corrected the bridge artifact hash and refreshed the stale post-merge
continuation metadata, and re-verified the six source baselines with zero drift. Step 00's overall
closure remains blocked by the authoritative five `NeedsRecheck` source overlays; this record does
not convert that state to completion.
