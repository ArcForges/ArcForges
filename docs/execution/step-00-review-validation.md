# Step 00 Review Validation Evidence

This record belongs to the local review continuation of Step 00. It is docs-only evidence; no
source repository, solution, project, C#/XAML, build, CI, executable, service, installer, updater,
or subprocess was changed or launched.

## Repository and policy checks

- Review branch: `feat/af00-scope-source-inventory-reboot` (the supplied original branch).
- Review worktree: `C:\MyFile\ArcForges\ArcForges\.worktree\af00-scope-source-inventory-reboot`.
- Review parent: `ed6a89494389df4b7ece643e235b3816866e0acb`.
- Source baseline authority remains the six frozen commits recorded in `docs/scope/source-baseline.md`;
  the three declared dirty overlays have DiffSha256 values recorded in the planning-repository
  matrix writeback `1a918eb9f2f287139c642bcf247662bbae9d7d9a`.
- `git ls-files '*.ps1' '*.sh'` → empty output (tracked-helper-script policy satisfied).
- `git diff --check` → no output (whitespace check passed).

## Source and inventory assertions

- The current target `docs/scope/source-subsystems.md` has 41 ipcBridge export-group rows.
- The group declarations contain 315 named member tokens; the group index is explicitly not the
  Feature denominator. The final atomic container remains the planning-repository
  `feature-inventory-and-mapping.md`.
- The six-source baseline and dirty-file assertions were performed as read-only one-time
  PowerShell assertions because the repository policy forbids retaining the plan-mandated helper
  scripts. The recorded state is: six frozen HEADs match; AFFiNE, siyuan, and Serial-Studio are
  clean; AionUi, ArcVideo, and ArcVideoFoundation match their declared dirty-file sets and hashes.
- Dirty feature rows remain `NeedsRecheck`: `AF-F-AIONUI-0283`, `AF-F-AIONUI-0285`,
  `AF-F-ARCV-0065`, `AF-F-ARCV-0069`, and `AF-F-ARCVF-0011`; coverage rows remain
  `SC-AION-03`, `SC-AION-05`, `SC-AV-01`, `SC-AV-04`, and `SC-AVF-02`.

## Packaged oracle

- Static inventory root: `C:\MyFile\ArcForges\StartArcForges`.
- Status: `NotExecuted`; no executable, service, installer, updater, or subprocess was launched.
- Full-tree inventory: 11,297 regular files, 4,172,582,286 bytes.
- Deterministic sorted `product-relative-path|file-length` manifest SHA-256:
  `0e16e92490e1986b71f2b0186ee0a0fe4212119e5eade4b0b3d7f35cd95b143d`.
- Product counts and selected metadata are recorded in
  `docs/scope/packaged-oracle-static-evidence.md`.

## Traceability bridge

- The old ignored local bridge was read and rejected as stale: 24 records, 86 feature IDs, 27
  coverage IDs, abbreviated baselines, null evidence hashes, and placeholder feature IDs.
- A replacement was generated by an inline read-only PowerShell assertion from the current plan
  Feature inventory and Coverage register (the generator is intentionally not persisted because
  tracked `.ps1` files are prohibited).
- Replacement result: `records=24`, `featureIds=833`, `coverageIds=27`, `missing=0`, `extra=0`,
  `closureState=BridgeGenerationRequired`; all rows remain honest `NeedsRecheck`/unclosed until
  their referenced requirement, test, gate, and source review evidence exists.
- Local artifact SHA-256:
  `e7e5768f879ce5607f0fcf37a504c433b3e9423a76481f620dd107f74834b41`.
- Artifact path: `artifacts/evidence/traceability/feature-trace-bridge.json`.

## Planning-repository writeback

- `license-and-reuse-matrix.md` §1 was corrected to reflect the three dirty source worktrees and
  their exact DiffSha256 values. The separate planning worktree is clean at commit
  `1a918eb9f2f287139c642bcf247662bbae9d7d9a`.
- No PR was opened for the planning repository or the target repository.

## Gate result

The document, license, manifest, oracle, sequencing, and bridge structure review findings were
reconciled locally. Step 00 remains blocked by the authoritative five `NeedsRecheck` source
overlays; this record does not convert that state to completion.
